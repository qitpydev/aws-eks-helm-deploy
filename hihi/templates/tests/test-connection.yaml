apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hihi.fullname" . }}-test-connection"
  labels:
    {{- include "hihi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hihi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
