apiVersion: v1
kind: Service
metadata:
  name: {{ include "aaaa.fullname" . }}
  labels:
    {{- include "aaaa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aaaa.selectorLabels" . | nindent 4 }}
